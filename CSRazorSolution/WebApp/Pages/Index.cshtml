@page
@model IndexModel // tie to the C# webpage process model page (in index.cshtml.cs) => as class name
@{ // C# code block - c# silence statement
    ViewData["Title"] = "Home page"; // Go to html and comeback in c# world
}

    <h1>Welcome to Razor Pages in CPSC-1517</h1>
    <p>Learn about <a href="https://docs.microsoft.com/aspnet/core">building Web apps with ASP.NET Core</a>.</p>
    <br/><br/>
    @if(string.IsNullOrWhiteSpace(Model.MyName))
        {
            <p>The world of Razor is currently closed</p>
        }
        else
        {
            <p style="color:forestgreen; font-weight:bold;">@Model.MyName</p>
        }
